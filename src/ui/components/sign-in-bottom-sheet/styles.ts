import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  heading: {
    letterSpacing: -0.32,
  },
  container: {
    paddingHorizontal: 24,
  },
  form: {
    gap: 16,
    marginTop: 24,
  },
})
